<h1>Welcome to Our Page</h1>

<% @posts.each do |post| %>
	<div>
		<h2><%= post.title %></h2>
		<p> <%= post.description %> </p>
		<p> <%= post.author %> </p>
		<a href="/posts/<%= post.id %>">Show</a>
	</div>
<% end %>
